/*27. Crie um laço de repetição que exiba os números de 20 à 0.
*/

void main() {
  for (int contador = 20; contador != -1; contador--) {
    print(contador);
  }

  for (int contador = 20; contador > 0; contador--) {
    print(contador);
  }
}
